* Ansible
  - generate output for setting up the rules
  - generate deletion code for cleaning up the rules
* nftables:
  - Add output format nftables
  - Add deletion format nftables
  - Add output format nftables-rules? Is there an equivalent?
* Monitor mode: query the api-server for NodePorts with a certain
  selector
